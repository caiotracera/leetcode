name: "Run tests"
on: [push, pull_request]

jobs:
  run-tests:
    name: "Run tests"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: "Install dependencies"
        run: npm ci
      - name: "Run tests"
        run: npm test
